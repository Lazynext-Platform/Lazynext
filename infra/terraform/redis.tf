# ── Redis Caching ─────────────────────────────────────────────────────────
# Rate limiting and session caching handled via Upstash Redis (UPSTASH_REDIS_URL env var).
# Azure Cache for Redis is being retired — migrated to managed Upstash.
# No Azure Redis resources provisioned in this deployment.
