#!/bin/bash
# Generate placeholder icons using ImageMagick or sips (macOS built-in)
# sips -z 16 16 -c 16 16 --setProperty format png icon.svg --out icon16.png
echo "Run: npx @aspect-build/generate or create icons manually in public/icons/"
