/**
 * Audio DSP constants shared across the timeline and audio pipeline.
 *
 * @module timeline/audio-constants
 */

/** Minimum audio volume in decibels (effectively silent). */
export const VOLUME_DB_MIN = -60;

/** Maximum audio volume in decibels. */
export const VOLUME_DB_MAX = 20;
