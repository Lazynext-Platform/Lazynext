/**
 * Fixed timeline scale constants.
 *
 * @module timeline/scale
 */

/** Baseline CSS pixels per second of timeline content at 1× zoom. */
export const BASE_TIMELINE_PIXELS_PER_SECOND = 50;

/** Minimum zoom level (0.1 = 10% of baseline). */
export const TIMELINE_ZOOM_MIN = 0.1;

/** Maximum zoom level (100× baseline). */
export const TIMELINE_ZOOM_MAX = 100;
