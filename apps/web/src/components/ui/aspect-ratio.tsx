"use client";

/** @component Locks child content at a given width/height ratio so it scales responsively. */

import { AspectRatio as AspectRatioPrimitive } from "radix-ui";

const AspectRatio = AspectRatioPrimitive.Root;

export { AspectRatio };
