/**
 * @module stickers/intrinsic-size
 * @description Sticker intrinsic size resolution.
 */

/** Default size in pixels used when intrinsic dimensions are unknown. */
export const STICKER_INTRINSIC_SIZE_FALLBACK = 200;
