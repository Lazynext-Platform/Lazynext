/**
 * @module Audio transcription processing constants — sample rate, chunk
 * length, and stride for the real-time transcription pipeline.
 */

export const DEFAULT_TRANSCRIPTION_SAMPLE_RATE = 16000;

/** Utility representing DEFAULT_CHUNK_LENGTH_SECONDS. */
export const DEFAULT_CHUNK_LENGTH_SECONDS = 30;
/** Utility representing DEFAULT_STRIDE_SECONDS. */
export const DEFAULT_STRIDE_SECONDS = 5;
