/** @module Default frame rate constant for new projects (30 fps) */
import type { FrameRate } from "lazynext-wasm";

/** Utility representing DEFAULT_FPS. */
export const DEFAULT_FPS: FrameRate = { numerator: 30, denominator: 1 };
