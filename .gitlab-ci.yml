# DEPRECATED — 2026-06-30
# Lazynext primary CI runs on GitHub Actions (.github/workflows/ci.yml).
# This GitLab CI file is retained for archival reference only.
#
# For current CI configuration, see: .github/workflows/

stages:
  - message

deprecated-notice:
  stage: message
  script:
    - echo "This GitLab CI file is deprecated. Use GitHub Actions for CI/CD."
    - echo "See: .github/workflows/ci.yml"
