[package]
name = "lazynext_mcp_server"
version = "0.1.0"
edition = "2021"

[dependencies]
lazynext_core = { path = "../core" }
tokio = { version = "1.0", features = ["rt-multi-thread", "macros", "sync"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
