[package]
name = "ffmpeg_filter"
version = "0.1.0"
edition = "2024"
description = "FFMPEG filter_complex graph builder and rendering engine for Lazynext NLE"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
