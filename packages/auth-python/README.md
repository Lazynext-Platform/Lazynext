# Lazynext Auth (Python)

Shared JWT authentication module for Lazynext Python services.

## Overview

Provides JWT token verification, session validation, and auth middleware for FastAPI microservices (pre-processing, generative-studio). Integrates with the Better Auth token format used across the Lazynext platform.

## Installation

```bash
pip install lazynext-auth
```
