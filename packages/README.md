# Packages — Shared Libraries

Shared TypeScript packages used across apps and services.

| Package | Purpose |
|---------|---------|
| `api-client/` | Type-safe REST client for the API Gateway |

## Usage

```json
{
  "dependencies": {
    "@lazynext/api-client": "workspace:*"
  }
}
```
