name: Lazynext CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  hello:
    name: Sanity Check
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "Workflow works!"
