# LEGACY — Use services/ai-agents/Dockerfile for new deployments
# Sync Service (WebRTC signaling + CRDT sync)
FROM oven/bun:1-alpine

WORKDIR /app

COPY services/ai-agents/package.json ./
RUN bun install

COPY services/ai-agents/ ./

EXPOSE 8002

CMD ["bun", "run", "start"]
