# Kubernetes Secrets Manifest
# Template only — actual values are stored in Sealed Secrets (sealed-secrets.yaml).
# After ExternalSecret syncs, sensitive values are populated into K8s secrets.

apiVersion: v1
kind: Secret
metadata:
  name: lazynext-secrets
  namespace: lazynext
  labels:
    app.kubernetes.io/part-of: lazynext
    app.kubernetes.io/managed-by: kustomize
type: Opaque
stringData:
  # Template values — actual secrets managed via sealed-secrets.yaml
  # DATABASE_URL: postgresql://...
  # BETTER_AUTH_SECRET: synced from secrets
  # UPSTASH_REDIS_REST_TOKEN: synced from secrets
  # GEMINI_API_KEY: synced from secrets
  # MARBLE_WORKSPACE_KEY: synced from secrets
  # FREESOUND_CLIENT_ID: synced from secrets
  # FREESOUND_API_KEY: synced from secrets
  # DODO_API_KEY: synced from secrets
  # DODO_WEBHOOK_SECRET: synced from secrets
  # RESEND_API_KEY: synced from secrets
