# Production environment variables — TEMPLATE
# Copy this file to env.production.yaml and fill in real values.
# NEVER commit env.production.yaml to version control.
# See .env.example for the full list of supported variables.

NODE_ENV: production
NEXT_PUBLIC_SITE_URL: "https://lazynext.com"
DATABASE_URL: "postgresql://REPLACE_USER:REPLACE_PASSWORD@REPLACE_HOST:5432/REPLACE_DB?sslmode=require"
BETTER_AUTH_SECRET: "REPLACE_WITH_RANDOM_64_CHAR_SECRET"
BETTER_AUTH_URL: "https://lazynext.com"
LLM_PROVIDER: "anthropic"
STORAGE_PROVIDER: "azure"
MEDIA_BUCKET: "media"
# API keys — set only those you need; services gracefully degrade without them
# OPENAI_API_KEY: "sk-..."
# ANTHROPIC_API_KEY: "sk-ant-..."
# REPLICATE_API_TOKEN: "r8_..."
# ELEVENLABS_API_KEY: "..."
# PEXELS_API_KEY: "..."
# RESEND_API_KEY: "re_..."
# STRIPE_WEBHOOK_SECRET: "whsec_..."
