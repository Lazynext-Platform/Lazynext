# ── Lazynext Ansible Galaxy Requirements ──────────────────────────
# Install required collections and roles before running playbooks:
#   ansible-galaxy install -r requirements.yml
---
collections:
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: kubernetes.core
    version: ">=5.0.0"
  - name: community.postgresql
    version: ">=3.0.0"
