# ── Lazynext NVIDIA GPU Role Handlers ────────────────────────────────────
# Restart Docker daemon after NVIDIA container toolkit installation.
---
- name: Restart Docker
  systemd:
    name: docker
    state: restarted
