# ── Lazynext Docker Role Handlers ────────────────────────────────────────
# Restart containerd after Docker daemon config changes.
---
- name: restart containerd
  ansible.builtin.systemd:
    name: containerd
    state: restarted
