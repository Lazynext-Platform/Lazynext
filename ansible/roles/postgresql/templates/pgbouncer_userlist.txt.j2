# ── PgBouncer User List Template ────────────────────────────────────
# Defines the admin user and password for PgBouncer connection pooling.
"{{ postgresql_admin_user }}" "{{ postgresql_admin_password }}"
