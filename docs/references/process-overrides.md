# Process Overrides

> Custom process customizations that deviate from the standard Mastery framework.

---

## N/A — No Overrides

This project follows the standard Mastery framework without customizations. All process rules are defined in `docs/mastery.md` and `docs/mastery-compact.md`.
