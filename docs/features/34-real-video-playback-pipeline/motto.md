# 🧭 Motto: Real Video Playback Pipeline

> **Feature**: `34` — Real Video Playback Pipeline
> **Branch**: `feature/34-real-video-playback-pipeline`
> **Last Updated**: 2026-07-01

---

## North Star

The platform renders real video content end-to-end — ingest a real .mp4, see real frames in the preview, and export a real video file verified by ffprobe.

---

## DO ✅

- Wire existing components — ffmpeg_loader, ring_buffer_decoder, FrameCacheLoader all exist
- Test with real .mp4 files generated by ffmpeg
- Verify every output with ffprobe
- CLI path first (simplest Rust→Rust connection)
- Keep test_pattern as documented fallback

## DON'T ❌

- Don't build a new video decoder — ffmpeg_loader already wraps libavcodec
- Don't modify the compositor pipeline — it renders whatever textures it receives
- Don't touch the CRDT state — this is about rendering, not editing
- Don't add new dependencies — everything needed is already in Cargo.toml

## Boundaries 🚧

- Only modify files listed in architecture.md
- CLI changes only in cmd_render function
- Desktop changes only in editor.rs render loop
- Web changes only in wasm-bridge.ts

## Success Looks Like 🎯

- `lazynext_cli render test_project` produces output.mp4 with ffprobe showing real content (not solid color)
- Desktop app shows video frames instead of "No Frame Rendered"
- Web app renders uploaded .mp4 in browser canvas
