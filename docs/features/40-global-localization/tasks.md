# Tasks: Global Localization

- [ ] Update Database Schema (`drizzle/schema.ts`) with `locale` and `country` fields on the `user` table.
- [ ] Run drizzle migration generation and apply to db.
- [ ] Setup `next-intl` in Web App (`apps/web`).
- [ ] Setup `rust-i18n` in API Gateway (`rust/api-gateway`).
- [ ] Setup `react-i18next` in Mobile App (`apps/mobile`).
- [ ] Setup Fluent/i18n in Desktop App (`apps/desktop`).
- [ ] Setup `chrome.i18n` in Browser Extension (`apps/browser-extension`).
- [ ] Map `sys-locale` in CLI (`rust/cli`).
- [ ] Update MCP Server to accept locale params (`services/mcp-server`).