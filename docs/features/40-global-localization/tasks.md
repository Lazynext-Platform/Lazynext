# Tasks: Global Localization

- [x] Update Database Schema (`drizzle/schema.ts`) with `locale` and `country` fields on the `user` table.
- [x] Run drizzle migration generation and apply to db.
- [x] Setup `next-intl` in Web App (`apps/web`).
- [x] Setup `rust-i18n` in API Gateway (`rust/api-gateway`).
- [x] Setup `react-i18next` in Mobile App (`apps/mobile`).
- [x] Setup Fluent/i18n in Desktop App (`apps/desktop`).
- [x] Setup `chrome.i18n` in Browser Extension (`apps/browser-extension`).
- [x] Map `sys-locale` in CLI (`rust/cli`).
- [x] Update MCP Server to accept locale params (`services/mcp-server`).